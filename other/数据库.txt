
SQL：

创建数据库 create database NAME;

创建数据表
create table TABLENAME(
    属性名 数据类型 [约束条件],
    ...
)

非空约束：该字段不能为空
    属性名 数据类型 not null;

唯一性约束：该字段在表中不会重复
    属性名 数据类型 unique;




主键：可以唯一表示一个数据表中的一条数据
    一个表中，的主键必须是唯一的
    主键可以是单字段主键，也可以是多字段主键

